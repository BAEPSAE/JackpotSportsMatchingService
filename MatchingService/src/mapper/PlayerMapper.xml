<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.PlayerMapper">

	<!-- 개인정보 가져오기 -->
	<select id="getUserInfo" parameterType="String" resultType="Player">
	
	</select>
	
	<!-- 종목별 전적 가져오기 -->
	<select id="getUserRecord" parameterType="Map" resultType="Record">
	
	</select>
	
	<!-- 종목별 경기장 정보 가져오기 -->
	<select id="getUserGround" parameterType="Map" resultType="Grounds">
	
	</select>
	
</mapper>
